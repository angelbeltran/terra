import initialState from '../initial-state'
/*
import { 
} from '../constants'
*/

export default function rootReducer(state = initialState, action) {
  switch (action.type) {

    default:
      return state
  }
}
